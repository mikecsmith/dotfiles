add_newline = false

format = """
[╭─](fg:244)$os$username$hostname$directory$git_branch$git_status$fill$cmd_duration$time
[╰─](fg:244)$character"""

# Fill with dots
[fill]
symbol = "·"
style = "fg:244"

# -----------------------------------------------------------------------------
# OS SYMBOLS (Mac & Linux)
# -----------------------------------------------------------------------------
[os]
disabled = false
style = "fg:255"
format = "[$symbol ]($style)"

[os.symbols]
Macos = ""
Ubuntu = ""
Debian = ""
Fedora = ""
Arch = ""
CentOS = ""
Alpine = ""
Linux = ""   # Generic fallback for other distros
Windows = ""

# -----------------------------------------------------------------------------
# DIRECTORY & GIT
# -----------------------------------------------------------------------------

[directory]
style = "bold fg:31"                              # Added 'bold' to match p10k style
read_only = " 🔒"
truncation_length = 3
format = "[ $path]($style)[$read_only]($style) "

[git_branch]
symbol = " "
style = "fg:76"
format = "[$symbol$branch]($style) "

# -----------------------------------------------------------------------------
# GIT STATUS PALETTE
# -----------------------------------------------------------------------------
# SYMBOL   MEANING                 COLOR           CODE
# +        Staged (Ready)          Green           76
# !        Modified (Changed)      Gold            178
# ?        Untracked (New)         Blue            39
# »        Renamed (Moved)         Purple          135
# ✘        Deleted (Removed)       Red             160
# *        Stashed (Saved)         Cyan            43
# -----------------------------------------------------------------------------
#
[git_status]
style = "fg:76"
format = '($all_status$ahead_behind )'
conflicted = "[~${count}](fg:196) "
ahead = "[⇡${count}](fg:76)"
behind = "[⇣${count}](fg:76)"
diverged = "⇡${ahead_count}⇣${behind_count}"
stashed = "[*${count}](fg:43) "
untracked = "[?${count}](fg:39) "
staged = "[+${count}](fg:76) "
modified = "[!${count}](fg:178) "
renamed = "[»${count}](fg:134) "
deleted = "[✘${count}](fg:196) "

up_to_date = ""

# -----------------------------------------------------------------------------
# RIGHT SIDE INFO
# -----------------------------------------------------------------------------

[username]
show_always = false
style_user = "fg:180"
style_root = "bold fg:178"
format = "[$user]($style)@"

[hostname]
ssh_only = true
style = "fg:180"
format = "[$hostname]($style) "

[cmd_duration]
min_time = 3000
style = "fg:101"
format = "took [$duration]($style) "

[time]
disabled = false
time_format = "%I:%M:%S %p"
style = "fg:66"
format = "at [$time]($style)"

[character]
success_symbol = "[❯](bold fg:76)"
error_symbol = "[❯](bold fg:196)"
vicmd_symbol = "[❮](bold fg:76)"
